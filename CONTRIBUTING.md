# Contributing to xiaohongshu-copywriter

Keep changes focused. For behavior changes, provide a natural-language trigger scenario that fails before the change and passes after it. Run `python scripts/validate_repository.py`, validate `SKILL.md`, and update documentation when behavior or installation changes.

Bug reports should include the Agent/runtime, operating system, exact prompt, expected behavior, observed behavior, and relevant non-secret logs.
